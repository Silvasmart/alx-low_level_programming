#include <stdio.h>
/**
 * main - scripts
 * Description: 'prints all single digit numbers of base 10 starting from 0'
 * Return: always 0
 */
int main(void)
{
	int n;

	while (n < 10)
	{
		printf("%d\n", n);
		n++;
	}
	return (0);
}
